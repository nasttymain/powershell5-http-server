# PowerShell5-HTTP-Server
Simple HTTP server written with pure PS1 script.

### Features
- HTTP/1.0
  - Supports GET method
  - HEAD not implemented yet
- Can respond with files under `root/`
  - Resistant to directory traversal